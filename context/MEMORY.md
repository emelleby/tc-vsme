<!-- Cap: 2,500 chars. Agent maintains via memory-write instructions. -->
# Working Memory

## Active Threads

## Environment Notes

- `bun convex dev` must be running during local development. Convex dev deployments are paused without it — no functions execute and the app silently fails to fetch data.

## Reference Docs

- `docs/AUTH.md` — authoritative doc for all auth-related work (Clerk + Convex integration). Always read it first when debugging or implementing auth. Keep it up to date when auth changes.

## Pending Decisions
